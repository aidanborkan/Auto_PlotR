## Features
- Upload a `.rds` containing a **list of data frames**
- Plot **Abundance vs Temperature** curves
- Choose which data frame(s) to plot (All or a specific one)
- Choose `collapsed_uniprot`:
  - Use the first entry in each data frame, **or**
  - Select a value from a dropdown
- Pagination to avoid rendering very large plot sets at once
